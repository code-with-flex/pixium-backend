# Redis (canvas cache, wired up in this change — shares the same
# instance/key as the indexer's canvas cache)
REDIS_URL=redis://localhost:6379

PORT=3000

# Not wired up yet — reserved for follow-up slices (users, leaderboard,
# quests, factions)
DATABASE_URL=postgresql://user:password@localhost:5432/pixium
